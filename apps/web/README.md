# CreatoMatch Web App

Next.js app for the CreatoMatch influencer / UGC platform.

```bash
pnpm install
pnpm db:push
pnpm db:seed
pnpm dev
```

Demo persona switcher is in the sidebar (Brand ↔ Creator).
